import 'package:flutter/material.dart';

class SiteEmptyState extends StatelessWidget {
  const SiteEmptyState({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('لا توجد مواقع محفوظة بعد.'));
}